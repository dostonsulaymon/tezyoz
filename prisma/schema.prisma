generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id        String  @id @default(auto()) @map("_id") @db.ObjectId
  email     String  @unique
  firstName String?
  lastName  String?
  username  String?
  password  String?

  status UserStatus @default(IN_REGISTRATION)

  mails Mail[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("user")
}

model Mail {
  id        String     @id @default(auto()) @map("_id") @db.ObjectId
  otp       String
  email     String
  status    MailStatus @default(PENDING)
  userId    String     @db.ObjectId
  user      User       @relation(fields: [userId], references: [id])
  expiresAt DateTime
  attempts  Int        @default(0)
  usedAt    DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("mail")
}

enum MailStatus {
  PENDING
  FAILED
  EXPIRED
  VERIFIED
}

enum UserStatus {
  IN_REGISTRATION // User created, waiting for OTP/email verification
  ACTIVE // Fully registered and verified
  SUSPENDED // Temporarily blocked
  DELETED // Soft deleted or removed
}
